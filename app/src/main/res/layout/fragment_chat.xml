<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Fragmenter.ChatFragment"
    android:id="@+id/chat_frag"
    android:background="@color/backgroundRed">

    <!-- TODO: Update blank fragment layout -->

    <TextView
        android:id="@+id/chat_TextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="50dp"
        android:layout_marginTop="260dp"
        android:layout_marginEnd="50dp"
        android:textAlignment="center"
        android:textSize="18dp"
        android:text="du kan trykke på chat-knappen for at gå videre til en gruppechatside"
        android:textColor="@color/goldButton"
        android:gravity="center_horizontal" />

    <Button
        android:id="@+id/Chat_knap"
        android:layout_width="140dp"
        android:layout_height="56dp"
        android:layout_marginStart="110dp"
        android:layout_marginTop="350dp"
        android:layout_marginEnd="110dp"
        android:background="@drawable/custom_rounded_corners"
        android:gravity="center"
        android:text="chat"
        android:textColorHint="@color/darkRedButtonText" />

</FrameLayout>